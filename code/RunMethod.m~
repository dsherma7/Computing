function err = RunMethod(meth,y,dy,a,b,ns)
y0 = y(a);
leg = {'y(t)'};
ttl_err = [];
for n = ns
    [yt,t] = meth(f,y0,a,b,n);
    plot(t,yt);
    leg{end+1} = sprintf('h=%d',round((b-a)/n,4));
    err(end+1) = sum(abs(y(t)-yt))/length(t);
end
title('Eulers Method')

end